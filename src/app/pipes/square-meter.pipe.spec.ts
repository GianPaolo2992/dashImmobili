import { SquareMeterPipe } from './square-meter.pipe';

describe('SquareMeterPipe', () => {
  it('create an instance', () => {
    const pipe = new SquareMeterPipe();
    expect(pipe).toBeTruthy();
  });
});
